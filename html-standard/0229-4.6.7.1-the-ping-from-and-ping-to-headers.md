# 4.6.7.1 The `Ping-From` and `Ping-To` headers

[Source](https://html.spec.whatwg.org/#the-ping-headers)

The `Ping-From` and `Ping-To` HTTP request headers are included in hyperlink
auditing requests. Their value is a URL, serialized.
